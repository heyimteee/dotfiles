{{- $email := promptString "email" -}}
{{- $name := promptString "name" -}}

[data]
    name = {{ $name | quote }}
    email = {{ $email | quote }}
    os = {{ .chezmoi.os | quote }}
    arch = {{ .chezmoi.arch | quote }}
